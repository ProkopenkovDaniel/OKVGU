@model OKVGU.Models.Specialty
@{
    Layout = null;
}
<div class="modal-content">
    <div class="modal-header">
        <button class="close" data-dismiss="modal" area-hidden="true">X</button>
        <h4>Группа</h4>
    </div>
    <div class="modal-body">
        @using (Html.BeginForm("AddSpecialty", "Home", FormMethod.Post, new { @class = "form-horizontal validate", role = "form" }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary()
            @Html.HiddenFor(m => m.id)
            <div class="form-group">
                <label class="col-md-4 control-label">Название</label>
                <div class="col-md-8">
                    @Html.TextBoxFor(m => m.Title, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <label class="col-md-4 control-label">Педагогическая</label>
                <div class="col-md-8">
                    @Html.CheckBoxFor(m => m.IsPedSpec, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-offset-4 col-md-8">
                    <input type="submit" class="btn btn-default" value="Добавить" />
                </div>
            </div>
        }
    </div>
</div>

